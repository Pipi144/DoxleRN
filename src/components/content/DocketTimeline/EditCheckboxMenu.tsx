import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

type Props = {};

const EditCheckboxMenu = (props: Props) => {
  return (
    <View>
      <Text>EditCheckboxMenu</Text>
    </View>
  );
};

export default EditCheckboxMenu;

const styles = StyleSheet.create({});
