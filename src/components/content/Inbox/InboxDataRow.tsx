import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

type Props = {};

const InboxDataRow = (props: Props) => {
  return (
    <View>
      <Text>InboxDataRow</Text>
    </View>
  );
};

export default InboxDataRow;

const styles = StyleSheet.create({});
